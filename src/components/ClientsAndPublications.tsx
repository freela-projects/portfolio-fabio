import styled from "styled-components";

const ClientAndPublications = styled.div`
    width: calc(100%-300px);

    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    min-width: 100vw;
`

export { ClientAndPublications }